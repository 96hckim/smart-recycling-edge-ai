# ==============================================================================
# Smart Recycling Edge Vision Pipeline Dependencies
# Target Platform: NVIDIA Jetson Orin Nano (JetPack 6.x / Ubuntu 22.04 LTS / aarch64)
# ==============================================================================

# 1. 수치 연산 및 텐서 행렬 처리 (YOLO 전/후처리, 바운딩 박스 변환)
# NOTE: JetPack 및 TensorRT/PyCUDA C-API 호환성을 위해 NumPy 2.x 업그레이드 방지 (<2.0.0)
numpy>=1.23.0,<2.0.0

# 2. GPU Host Pinned Memory 및 비동기 CUDA Stream 파이프라인 제어
# NOTE: 설치 전 CUDA 컴파일러(nvcc) 환경변수 설정 필수: export PATH=/usr/local/cuda/bin:$PATH
pycuda>=2024.1

# 3. STM32 MCU 연동 UART 시리얼 I/O 제어
pyserial>=3.5

# 4. 컴퓨터 비전 영상 처리 (Letterbox 전처리, C++ 가속 NMS, JPEG 인코딩)
# NOTE: JetPack 시스템 OpenCV(python3-opencv) 사용 시 생략 가능, venv 격리 환경 시 설치 권장
opencv-python>=4.8.0

# ==============================================================================
# [선택 사항] 모델 변환 및 테스트 도구 (Model Export & Utilities)
# ==============================================================================
# YOLOv11 모델 PyTorch 가중치(.pt) -> ONNX 내보내기 시 필요
# ultralytics>=8.3.0

# ==============================================================================
# [시스템 레벨 종속성 - JetPack 기본 제공 (pip 설치 불필요)]
# ==============================================================================
# - NVIDIA TensorRT (10.x): python3-libnvinfer (JetPack 6.x 기본 탑재)
# - CUDA Toolkit (12.x): /usr/local/cuda
# ==============================================================================